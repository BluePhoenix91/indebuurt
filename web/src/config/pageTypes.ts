/**
 * Page type configuration
 * Controls which page types are generated during build
 */

export const PAGE_TYPE_CONFIG = {
  overview: { enabled: true },
  huizen: { enabled: true },
  demografie: { enabled: false }, // Enable when template has 300+ words of context
};
